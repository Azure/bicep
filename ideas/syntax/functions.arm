input myVariable string

// functions are pure and must return something directly - e.g. an object
function myFunction (input1 string) => {
  myVal: myVariable
  input1: input1
}

// or an array
function join (a string, b string) => [
  a
  b
]

// or even a string
function concat (a string, b string) => '${a}-${b}'